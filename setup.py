from setuptools import setup, find_packages


setup(name='jointrecog',
      version="0.0",
      packages=['jointrecog'])
setup(name='mp18-project-sekeleton',
      version="0.0",
      packes=find_packages())
